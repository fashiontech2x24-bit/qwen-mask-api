# Compatible requirements for SCHP Parser
# Use with PyTorch 1.12.0

# Core Dependencies (compatible with PyTorch 1.12)
torch==1.12.0
torchvision==0.13.0
Pillow>=8.0.0
numpy>=1.19.0,<1.24.0  # Compatible with PyTorch 1.12
opencv-python>=4.5.0
pyyaml>=5.4.0
tqdm>=4.60.0
ninja>=1.10.0  # Required for C++ extension compilation

# Development Dependencies (optional)
pytest>=6.0.0
pytest-cov>=2.12.0

# Note: Install PyTorch separately with CUDA support:
# pip install torch==1.12.0 torchvision==0.13.0 --extra-index-url https://download.pytorch.org/whl/cu113

